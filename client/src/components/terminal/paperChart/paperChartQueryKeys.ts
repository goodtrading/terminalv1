import { PAPER_INVALIDATE_KEYS } from "../execution/paperQueryKeys";

/** Same keys as Trading Execution — keeps chart ↔ panel ↔ reports in sync */
export const PAPER_CHART_INVALIDATE_KEYS = PAPER_INVALIDATE_KEYS;
